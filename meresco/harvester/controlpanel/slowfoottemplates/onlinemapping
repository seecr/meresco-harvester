<%
type('text/plain')
prepareHeaders('private')
from meresco.harvester.onlineharvest import OnlineHarvest
from meresco.harvester.mapping import Mapping
f = asform(input)
%>
Gebruikte url:
<%=f.inputurl%>

Gebruikte mapping:
<%
harvest = OnlineHarvest(req, req.proxy)

xmlurl = str(f.inputurl)
harvest.performMapping(domainId=args.domainId, mappingId=args.mappingId, urlString=xmlurl)
%>
